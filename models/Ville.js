class VilleModel {
    constructor(id, nom, description, image) {
        this.id = id || '';
        this.nom = nom || '';
        this.description = description || '';
        this.image = image || '';
    }
}

export default VilleModel;